FILES
infos.txt	shell commands for the server

CODE
flong.r		function to make long format data-set (strip the priors and deltas from the column names)
combine_data.r	after the parallel processing, combine the data files to one
loadlibs.r	load the necessary libraries to the server
wrapper.r	starts the simulation and paralellizes it on the server
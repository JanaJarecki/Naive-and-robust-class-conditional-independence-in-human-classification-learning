Note
The preprocessing canot be done because we cannot share the raw data online
this folder if only for archival porposes

Files

derandomiceClass.r	check whether class label is reversed and fix it
derandomizeStimuli.r	assign label (111) to stimuli format (fmo) according to frequency of 111 == freq of fmo
preprocessData.r	parse the raw data string and extract learning, clicking, estimates, knowledge, and duration data
standardizeString.r	standardize the subject raw data string subjext####.txt
wrapper.r		wraps the function and executes this --> use to preprocess the data